
        stage("Report") {
            steps {
                unstash 'reports'
                sh './report-kawa'
                archiveArtifacts artifacts: 'reports/*.html'
            }
        }

    }
    post {
        always {
            deleteDir()
        }
    }
}
